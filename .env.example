APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:fHfr135Sf4AzmueSup1oZv0NeMaIU9r9rQW1NMidwz8=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=root
DB_PASSWORD=

# HRIS database (evaluasi aplikasi)
DB_CONNECTION_HRIS=mysql
DB_HOST_HRIS=127.0.0.1
DB_PORT_HRIS=3306
DB_DATABASE_HRIS=hris_db_live
DB_USERNAME_HRIS=hris_user
DB_PASSWORD_HRIS=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# SONIA SSO — token harus sama dengan AGRINAV_SSO_TOKEN di aplikasi SONIA
SONIA_URL=https://sonia.ptpn1.co.id
SONIA_SSO_TOKEN=

# AI Backend Configuration
AI_BACKEND_URL=https://be.ptpn1.co.id/api/ai/chat
AI_BACKEND_TIMEOUT=120
AI_BACKEND_AGENT=agrinav_agent

# AI Backend Auth (login untuk dapat webChatToken; API Key/JWT lama tidak berlaku)
AI_BACKEND_BASE_URL=https://be.ptpn1.co.id
AI_BACKEND_USERNAME=your-username-here
AI_BACKEND_PASSWORD=your-password-here

# Puppeteer Automation Configuration
PUPPETEER_AUTH_TOKEN=ptpn1-puppeteer-automation-2027-xK9mPqRs
PUPPETEER_ALLOWED_IPS=127.0.0.1,::1,localhost,10.100.11.251
PUPPETEER_DEFAULT_USER=superadmin
PUPPETEER_SESSION_LIFETIME=120
PUPPETEER_ENABLE_LOGGING=true
PUPPETEER_RATE_LIMIT=100

# API IP Whitelist (dipakai middleware restrict.ip untuk endpoint API mesin/n8n)
# Dipisah koma. Mendukung exact, CIDR (10.0.0.0/24), wildcard (10.0.0.*), localhost.
# Kosongkan untuk menonaktifkan (fail-open). Isi dengan IP server n8n untuk mengaktifkan.
API_ALLOWED_IPS=127.0.0.1,::1,localhost,10.100.11.251,10.100.11.122,10.100.13.1
